name: Crear imagenes de Docker y push a Packages

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:

   build-docker-image-and-publish:
     runs-on: ubuntu-latest
     steps:
     - uses: actions/checkout@v3
     - name: Docker Login
       uses: docker/login-action@v1
       with:
         registry: ghcr.io
         username: ${{ github.actor }}
         password: ${{ secrets.GITHUB_TOKEN }}
     - name: Build and Publish Docker CLIENT
       id: docker_build_client
       uses: docker/build-push-action@v2
       with:
         context: client/.
         push: true
         tags: |
           ghcr.io/jaumoso/cromoteca/cromoteca-client:latest
     - name: Build and Publish Docker SERVER
       id: docker_build_server
       uses: docker/build-push-action@v2
       with:
         context: server/.
         push: true
         tags: |
           ghcr.io/jaumoso/cromoteca/cromoteca-server:latest
     - name: tags-versioning
     # You may pin to the exact commit or the version.
    # uses: georgeneto/tags-versioning@50eb0a3bfac7a6d055e2b41de87ee0b02dc33eeb
       uses: georgeneto/tags-versioning@v1
     - name: Print Image Digests
       run: |
         echo ${{ steps.docker_build.outputs.digests }}
